/* empty css                                 */
import { d as createComponent, e as renderTemplate, f as defineScriptVars, r as renderComponent, m as maybeRenderHead, g as addAttribute } from '../chunks/astro/server_ncvnUENx.mjs';
import 'kleur/colors';
import 'html-escaper';
import { s as servingOptions, t as timeOptions, d as dietaryOptions, c as cuisines, $ as $$Layout, r as recipesData, a as $$Image, g as getCuisineName, b as getTimeLabel } from '../chunks/recipes_BdHlcyN1.mjs';
import '@astrojs/internal-helpers/path';
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Index = createComponent(($$result, $$props, $$slots) => {
  const recipes = recipesData.recipes;
  return renderTemplate(_a || (_a = __template(["", " <script>(function(){", "\n  // State\n  let filters = {\n    cuisine: 'all',\n    budget: 500,\n    serves: null,\n    time: 'all',\n    dietary: [],\n    search: ''\n  };\n\n  // DOM Elements\n  const searchInput = document.getElementById('searchInput');\n  const budgetSlider = document.getElementById('budgetSlider');\n  const budgetValue = document.getElementById('budgetValue');\n  const filterToggle = document.getElementById('filterToggle');\n  const filterPanel = document.getElementById('filterPanel');\n  const clearFilters = document.getElementById('clearFilters');\n  const resultsCount = document.getElementById('resultsCount');\n  const noResults = document.getElementById('noResults');\n\n  // Cuisine buttons\n  document.querySelectorAll('.cuisine-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.cuisine-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'text-white');\n        b.classList.add('border-secondary', 'bg-card-bg');\n      });\n      btn.classList.add('bg-primary', 'text-white');\n      btn.classList.remove('border-secondary', 'bg-card-bg');\n      filters.cuisine = btn.dataset.cuisine;\n      filterRecipes();\n    });\n  });\n\n  // Budget slider\n  budgetSlider.addEventListener('input', (e) => {\n    filters.budget = parseInt(e.target.value);\n    budgetValue.textContent = `$${filters.budget}`;\n    filterRecipes();\n  });\n\n  // Serving buttons\n  document.querySelectorAll('.serving-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.serving-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'border-primary', 'bg-accent/10');\n      });\n      const isActive = btn.classList.contains('bg-primary');\n      if (isActive) {\n        btn.classList.remove('bg-primary', 'border-primary', 'bg-accent/10');\n        filters.serves = null;\n      } else {\n        btn.classList.add('bg-primary', 'border-primary', 'bg-accent/10');\n        filters.serves = parseInt(btn.dataset.serves);\n      }\n      filterRecipes();\n    });\n  });\n\n  // Time buttons\n  document.querySelectorAll('.time-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.time-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'text-white');\n        b.classList.add('border-secondary', 'bg-card-bg');\n      });\n      btn.classList.add('bg-primary', 'text-white');\n      btn.classList.remove('border-secondary', 'bg-card-bg');\n      filters.time = btn.dataset.time;\n      filterRecipes();\n    });\n  });\n\n  // Dietary buttons\n  document.querySelectorAll('.dietary-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      btn.classList.toggle('bg-primary');\n      btn.classList.toggle('text-white');\n      btn.classList.toggle('border-primary');\n      \n      if (btn.classList.contains('bg-primary')) {\n        filters.dietary.push(btn.dataset.dietary);\n      } else {\n        filters.dietary = filters.dietary.filter(d => d !== btn.dataset.dietary);\n      }\n      filterRecipes();\n    });\n  });\n\n  // Search\n  searchInput.addEventListener('input', (e) => {\n    filters.search = e.target.value;\n    filterRecipes();\n  });\n\n  // Filter toggle (mobile)\n  filterToggle.addEventListener('click', () => {\n    filterPanel.classList.toggle('hidden');\n  });\n\n  // Clear filters\n  clearFilters.addEventListener('click', () => {\n    filters = {\n      cuisine: 'all',\n      budget: 500,\n      serves: null,\n      time: 'all',\n      dietary: [],\n      search: ''\n    };\n    \n    budgetSlider.value = 500;\n    budgetValue.textContent = '$500';\n    searchInput.value = '';\n    \n    document.querySelectorAll('.cuisine-btn')[0].click();\n    document.querySelectorAll('.time-btn')[0].click();\n    document.querySelectorAll('.serving-btn, .dietary-btn').forEach(b => {\n      b.classList.remove('bg-primary', 'border-primary', 'text-white');\n    });\n    \n    filterRecipes();\n  });\n\n  // Filter function\n  function filterRecipes() {\n    const cards = document.querySelectorAll('.recipe-card');\n    let visibleCount = 0;\n    \n    cards.forEach(card => {\n      const cuisine = card.dataset.cuisine;\n      const budget = parseInt(card.dataset.budget);\n      const serves = parseInt(card.dataset.serves);\n      const time = parseInt(card.dataset.time);\n      const dietary = card.dataset.dietary.split(',').filter(d => d);\n      \n      let show = true;\n      \n      // Cuisine filter\n      if (filters.cuisine !== 'all' && cuisine !== filters.cuisine) {\n        show = false;\n      }\n      \n      // Budget filter\n      if (budget > filters.budget) {\n        show = false;\n      }\n      \n      // Serves filter\n      if (filters.serves) {\n        const option = servingOptions.find(s => s.id === filters.serves);\n        if (option) {\n          const [min, max] = option.id === 1 ? [1, 1] : option.id === 2 ? [2, 2] : option.id === 4 ? [3, 4] : [5, 100];\n          if (serves < min || serves > max) {\n            show = false;\n          }\n        }\n      }\n      \n      // Time filter\n      if (filters.time !== 'all') {\n        const option = timeOptions.find(t => t.id === filters.time);\n        if (option && time > option.max) {\n          show = false;\n        }\n      }\n      \n      // Dietary filter\n      if (filters.dietary.length > 0) {\n        const hasAll = filters.dietary.every(d => dietary.includes(d));\n        if (!hasAll) {\n          show = false;\n        }\n      }\n      \n      // Search filter\n      if (filters.search) {\n        const cardText = card.textContent.toLowerCase();\n        const ingredients = card.dataset.ingredients.toLowerCase();\n        const searchTerm = filters.search.toLowerCase();\n        \n        if (!cardText.includes(searchTerm) && !ingredients.includes(searchTerm)) {\n          show = false;\n        }\n      }\n      \n      if (show) {\n        card.classList.remove('hidden');\n        visibleCount++;\n      } else {\n        card.classList.add('hidden');\n      }\n    });\n    \n    // Update count\n    resultsCount.textContent = `Showing ${visibleCount} recipes`;\n    \n    // Show/hide no results\n    if (visibleCount === 0) {\n      noResults.classList.remove('hidden');\n    } else {\n      noResults.classList.add('hidden');\n    }\n  }\n\n  // Initialize\n  document.querySelectorAll('.cuisine-btn')[0].classList.add('bg-primary', 'text-white');\n  document.querySelectorAll('.cuisine-btn')[0].classList.remove('border-secondary', 'bg-card-bg');\n  document.querySelectorAll('.time-btn')[0].classList.add('bg-primary', 'text-white');\n  document.querySelectorAll('.time-btn')[0].classList.remove('border-secondary', 'bg-card-bg');\n\n  // Random dice picker\n  const diceBtn = document.getElementById('diceBtn');\n\n  diceBtn.addEventListener('click', () => {\n    const visibleCards = Array.from(document.querySelectorAll('.recipe-card:not(.hidden)'));\n\n    if (visibleCards.length === 0) return;\n\n    const randomCard = visibleCards[Math.floor(Math.random() * visibleCards.length)];\n\n    // Remove highlight from any previously highlighted card\n    document.querySelectorAll('.dice-highlight').forEach(el => {\n      el.classList.remove('dice-highlight');\n    });\n\n    // Scroll to the card\n    randomCard.scrollIntoView({ behavior: 'smooth', block: 'center' });\n\n    // Apply highlight\n    randomCard.classList.add('dice-highlight');\n\n    // Remove highlight after 2.5s so it can be triggered again\n    setTimeout(() => {\n      randomCard.classList.remove('dice-highlight');\n    }, 2500);\n  });\n\n  // Scroll to top button\n  const scrollTopBtn = document.getElementById('scrollTopBtn');\n  if (scrollTopBtn) {\n    window.addEventListener('scroll', () => {\n      if (window.scrollY > 300) {\n        scrollTopBtn.classList.remove('opacity-0', 'pointer-events-none');\n        scrollTopBtn.classList.add('opacity-100');\n      } else {\n        scrollTopBtn.classList.add('opacity-0', 'pointer-events-none');\n        scrollTopBtn.classList.remove('opacity-100');\n      }\n    });\n    scrollTopBtn.addEventListener('click', () => {\n      window.scrollTo({ top: 0, behavior: 'smooth' });\n    });\n  }\n})();<\/script>"], ["", " <script>(function(){", "\n  // State\n  let filters = {\n    cuisine: 'all',\n    budget: 500,\n    serves: null,\n    time: 'all',\n    dietary: [],\n    search: ''\n  };\n\n  // DOM Elements\n  const searchInput = document.getElementById('searchInput');\n  const budgetSlider = document.getElementById('budgetSlider');\n  const budgetValue = document.getElementById('budgetValue');\n  const filterToggle = document.getElementById('filterToggle');\n  const filterPanel = document.getElementById('filterPanel');\n  const clearFilters = document.getElementById('clearFilters');\n  const resultsCount = document.getElementById('resultsCount');\n  const noResults = document.getElementById('noResults');\n\n  // Cuisine buttons\n  document.querySelectorAll('.cuisine-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.cuisine-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'text-white');\n        b.classList.add('border-secondary', 'bg-card-bg');\n      });\n      btn.classList.add('bg-primary', 'text-white');\n      btn.classList.remove('border-secondary', 'bg-card-bg');\n      filters.cuisine = btn.dataset.cuisine;\n      filterRecipes();\n    });\n  });\n\n  // Budget slider\n  budgetSlider.addEventListener('input', (e) => {\n    filters.budget = parseInt(e.target.value);\n    budgetValue.textContent = \\`$\\${filters.budget}\\`;\n    filterRecipes();\n  });\n\n  // Serving buttons\n  document.querySelectorAll('.serving-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.serving-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'border-primary', 'bg-accent/10');\n      });\n      const isActive = btn.classList.contains('bg-primary');\n      if (isActive) {\n        btn.classList.remove('bg-primary', 'border-primary', 'bg-accent/10');\n        filters.serves = null;\n      } else {\n        btn.classList.add('bg-primary', 'border-primary', 'bg-accent/10');\n        filters.serves = parseInt(btn.dataset.serves);\n      }\n      filterRecipes();\n    });\n  });\n\n  // Time buttons\n  document.querySelectorAll('.time-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.time-btn').forEach(b => {\n        b.classList.remove('bg-primary', 'text-white');\n        b.classList.add('border-secondary', 'bg-card-bg');\n      });\n      btn.classList.add('bg-primary', 'text-white');\n      btn.classList.remove('border-secondary', 'bg-card-bg');\n      filters.time = btn.dataset.time;\n      filterRecipes();\n    });\n  });\n\n  // Dietary buttons\n  document.querySelectorAll('.dietary-btn').forEach(btn => {\n    btn.addEventListener('click', () => {\n      btn.classList.toggle('bg-primary');\n      btn.classList.toggle('text-white');\n      btn.classList.toggle('border-primary');\n      \n      if (btn.classList.contains('bg-primary')) {\n        filters.dietary.push(btn.dataset.dietary);\n      } else {\n        filters.dietary = filters.dietary.filter(d => d !== btn.dataset.dietary);\n      }\n      filterRecipes();\n    });\n  });\n\n  // Search\n  searchInput.addEventListener('input', (e) => {\n    filters.search = e.target.value;\n    filterRecipes();\n  });\n\n  // Filter toggle (mobile)\n  filterToggle.addEventListener('click', () => {\n    filterPanel.classList.toggle('hidden');\n  });\n\n  // Clear filters\n  clearFilters.addEventListener('click', () => {\n    filters = {\n      cuisine: 'all',\n      budget: 500,\n      serves: null,\n      time: 'all',\n      dietary: [],\n      search: ''\n    };\n    \n    budgetSlider.value = 500;\n    budgetValue.textContent = '$500';\n    searchInput.value = '';\n    \n    document.querySelectorAll('.cuisine-btn')[0].click();\n    document.querySelectorAll('.time-btn')[0].click();\n    document.querySelectorAll('.serving-btn, .dietary-btn').forEach(b => {\n      b.classList.remove('bg-primary', 'border-primary', 'text-white');\n    });\n    \n    filterRecipes();\n  });\n\n  // Filter function\n  function filterRecipes() {\n    const cards = document.querySelectorAll('.recipe-card');\n    let visibleCount = 0;\n    \n    cards.forEach(card => {\n      const cuisine = card.dataset.cuisine;\n      const budget = parseInt(card.dataset.budget);\n      const serves = parseInt(card.dataset.serves);\n      const time = parseInt(card.dataset.time);\n      const dietary = card.dataset.dietary.split(',').filter(d => d);\n      \n      let show = true;\n      \n      // Cuisine filter\n      if (filters.cuisine !== 'all' && cuisine !== filters.cuisine) {\n        show = false;\n      }\n      \n      // Budget filter\n      if (budget > filters.budget) {\n        show = false;\n      }\n      \n      // Serves filter\n      if (filters.serves) {\n        const option = servingOptions.find(s => s.id === filters.serves);\n        if (option) {\n          const [min, max] = option.id === 1 ? [1, 1] : option.id === 2 ? [2, 2] : option.id === 4 ? [3, 4] : [5, 100];\n          if (serves < min || serves > max) {\n            show = false;\n          }\n        }\n      }\n      \n      // Time filter\n      if (filters.time !== 'all') {\n        const option = timeOptions.find(t => t.id === filters.time);\n        if (option && time > option.max) {\n          show = false;\n        }\n      }\n      \n      // Dietary filter\n      if (filters.dietary.length > 0) {\n        const hasAll = filters.dietary.every(d => dietary.includes(d));\n        if (!hasAll) {\n          show = false;\n        }\n      }\n      \n      // Search filter\n      if (filters.search) {\n        const cardText = card.textContent.toLowerCase();\n        const ingredients = card.dataset.ingredients.toLowerCase();\n        const searchTerm = filters.search.toLowerCase();\n        \n        if (!cardText.includes(searchTerm) && !ingredients.includes(searchTerm)) {\n          show = false;\n        }\n      }\n      \n      if (show) {\n        card.classList.remove('hidden');\n        visibleCount++;\n      } else {\n        card.classList.add('hidden');\n      }\n    });\n    \n    // Update count\n    resultsCount.textContent = \\`Showing \\${visibleCount} recipes\\`;\n    \n    // Show/hide no results\n    if (visibleCount === 0) {\n      noResults.classList.remove('hidden');\n    } else {\n      noResults.classList.add('hidden');\n    }\n  }\n\n  // Initialize\n  document.querySelectorAll('.cuisine-btn')[0].classList.add('bg-primary', 'text-white');\n  document.querySelectorAll('.cuisine-btn')[0].classList.remove('border-secondary', 'bg-card-bg');\n  document.querySelectorAll('.time-btn')[0].classList.add('bg-primary', 'text-white');\n  document.querySelectorAll('.time-btn')[0].classList.remove('border-secondary', 'bg-card-bg');\n\n  // Random dice picker\n  const diceBtn = document.getElementById('diceBtn');\n\n  diceBtn.addEventListener('click', () => {\n    const visibleCards = Array.from(document.querySelectorAll('.recipe-card:not(.hidden)'));\n\n    if (visibleCards.length === 0) return;\n\n    const randomCard = visibleCards[Math.floor(Math.random() * visibleCards.length)];\n\n    // Remove highlight from any previously highlighted card\n    document.querySelectorAll('.dice-highlight').forEach(el => {\n      el.classList.remove('dice-highlight');\n    });\n\n    // Scroll to the card\n    randomCard.scrollIntoView({ behavior: 'smooth', block: 'center' });\n\n    // Apply highlight\n    randomCard.classList.add('dice-highlight');\n\n    // Remove highlight after 2.5s so it can be triggered again\n    setTimeout(() => {\n      randomCard.classList.remove('dice-highlight');\n    }, 2500);\n  });\n\n  // Scroll to top button\n  const scrollTopBtn = document.getElementById('scrollTopBtn');\n  if (scrollTopBtn) {\n    window.addEventListener('scroll', () => {\n      if (window.scrollY > 300) {\n        scrollTopBtn.classList.remove('opacity-0', 'pointer-events-none');\n        scrollTopBtn.classList.add('opacity-100');\n      } else {\n        scrollTopBtn.classList.add('opacity-0', 'pointer-events-none');\n        scrollTopBtn.classList.remove('opacity-100');\n      }\n    });\n    scrollTopBtn.addEventListener('click', () => {\n      window.scrollTo({ top: 0, behavior: 'smooth' });\n    });\n  }\n})();<\/script>"])), renderComponent($$result, "Layout", $$Layout, { "title": "Home" }, { "default": ($$result2) => renderTemplate`  ${maybeRenderHead()}<header class="bg-card-bg border-b border-secondary sticky top-0 z-50"> <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between"> <div class="flex items-center gap-3"> <div class="w-10 h-10 bg-primary rounded-xl flex items-center justify-center"> <span class="text-white text-xl">🍳</span> </div> <h1 class="text-xl font-bold text-text">晚餐 recipes</h1> </div> <nav class="hidden md:flex items-center gap-6"> <a href="/-IThund-dinner-recipe/#recipes" class="text-text-muted hover:text-primary transition-colors">食譜</a> <a href="/-IThund-dinner-recipe/#about" class="text-text-muted hover:text-primary transition-colors">關於</a> </nav> </div> </header>  <section class="bg-secondary/30 py-16 md:py-24"> <div class="max-w-7xl mx-auto px-4 text-center"> <h2 class="text-4xl md:text-5xl font-bold text-text mb-4">
今晚煮咩好？
</h2> <p class="text-xl text-text-muted max-w-2xl mx-auto mb-8">
搵啱Budget、啱Time、啱口味嘅Recipes
</p> <!-- Search Bar --> <div class="max-w-xl mx-auto relative flex gap-3"> <div class="relative flex-1"> <input type="text" id="searchInput" placeholder="Search recipes... 搵食譜..." class="w-full px-6 py-4 bg-card-bg rounded-2xl border border-secondary focus:border-primary focus:ring-2 focus:ring-primary/20 outline-none transition-all text-lg"> <svg class="absolute right-4 top-1/2 -translate-y-1/2 w-6 h-6 text-text-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path> </svg> </div> <button id="diceBtn" class="px-5 py-4 bg-primary hover:bg-primary/90 text-white rounded-2xl border border-primary transition-all text-lg font-semibold flex items-center gap-2 shrink-0" title="Random pick! 隨機選擇"> <span class="text-xl">🎲</span> <span class="hidden sm:inline">隨機</span> </button> </div> </div> </section>  <section id="recipes" class="py-8 border-b border-secondary"> <div class="max-w-7xl mx-auto px-4"> <!-- Filter Toggle (Mobile) --> <button id="filterToggle" class="md:hidden w-full py-3 bg-card-bg rounded-xl border border-secondary flex items-center justify-center gap-2"> <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path> </svg> <span class="font-medium">Filters 篩選</span> </button> <!-- Filter Panel --> <div id="filterPanel" class="hidden md:block space-y-6"> <!-- Cuisine Filters --> <div> <h3 class="font-semibold text-text mb-3"> Cuisine 菜式</h3> <div class="flex flex-wrap gap-2"> <button class="cuisine-btn active px-4 py-2 rounded-full border-2 border-primary bg-primary text-white transition-colors" data-cuisine="all">
All 全部
</button> ${cuisines.map((c) => renderTemplate`<button class="cuisine-btn px-4 py-2 rounded-full border-2 border-secondary hover:border-primary transition-colors"${addAttribute(c.id, "data-cuisine")}> <span class="mr-1">${c.icon}</span> ${c.nameZh} </button>`)} </div> </div> <!-- Budget --> <div> <h3 class="font-semibold text-text mb-3">Budget Budget (HKD)</h3> <div class="flex items-center gap-4"> <span class="text-text-muted">$0</span> <input type="range" id="budgetSlider" min="0" max="500" value="500" class="flex-1 h-2 bg-secondary rounded-lg appearance-none cursor-pointer accent-primary"> <span class="text-text-muted">$500</span> <span id="budgetValue" class="text-primary font-semibold min-w-[60px]">$500</span> </div> </div> <!-- Serving Size --> <div> <h3 class="font-semibold text-text mb-3">Serves 份量</h3> <div class="flex flex-wrap gap-3"> ${servingOptions.map((s) => renderTemplate`<button class="serving-btn px-5 py-3 rounded-xl border-2 border-secondary hover:border-primary bg-card-bg transition-all flex items-center gap-2"${addAttribute(s.id, "data-serves")}> <span class="text-2xl">${s.icon}</span> <span class="font-medium">${s.nameZh}</span> </button>`)} </div> </div> <!-- Time --> <div> <h3 class="font-semibold text-text mb-3">Cooking Time 時間</h3> <div class="flex flex-wrap gap-2"> ${timeOptions.map((t) => renderTemplate`<button class="time-btn px-4 py-2 rounded-full border-2 border-secondary hover:border-primary bg-card-bg transition-colors"${addAttribute(t.id, "data-time")}> ${t.nameZh} ${t.id !== "all" && renderTemplate`<span class="text-text-muted text-sm ml-1">(${t.max} mins)</span>`} </button>`)} </div> </div> <!-- Dietary --> <div> <h3 class="font-semibold text-text mb-3">Dietary 飲食需求</h3> <div class="flex flex-wrap gap-2"> ${dietaryOptions.map((d) => renderTemplate`<button class="dietary-btn px-4 py-2 rounded-full border-2 border-secondary hover:border-primary bg-card-bg transition-colors"${addAttribute(d.id, "data-dietary")}> ${d.nameZh} </button>`)} </div> </div> <!-- Clear Filters --> <button id="clearFilters" class="text-primary hover:text-accent transition-colors">
Clear all filters 清除篩選
</button> </div> </div> </section>  <section class="py-4 border-b border-secondary"> <div class="max-w-7xl mx-auto px-4"> <p id="resultsCount" class="text-text-muted">
Showing ${recipes.length} recipes
</p> </div> </section>  <section class="py-8"> <div class="max-w-7xl mx-auto px-4"> <div id="recipeGrid" class="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3 sm:gap-6"> ${recipes.map((recipe) => renderTemplate`<article class="recipe-card bg-card-bg rounded-2xl overflow-hidden border border-secondary hover:border-primary hover:shadow-xl transition-all cursor-pointer"${addAttribute(recipe.cuisine, "data-cuisine")}${addAttribute(recipe.budget, "data-budget")}${addAttribute(recipe.serves, "data-serves")}${addAttribute(recipe.time, "data-time")}${addAttribute(recipe.dietary.join(","), "data-dietary")}${addAttribute(recipe.ingredients.join("|"), "data-ingredients")}> <a${addAttribute(`/-IThund-dinner-recipe/recipe/${recipe.slug}`, "href")} class="block"> <div class="aspect-[4/3] overflow-hidden bg-secondary/30"> ${renderComponent($$result2, "Image", $$Image, { "src": recipe.image, "alt": recipe.title, "width": 800, "height": 600, "class": "w-full h-full object-cover hover:scale-105 transition-transform duration-300", "loading": "lazy", "decoding": "async" })} </div> <div class="p-4"> <div class="flex items-center gap-2 mb-2"> <span class="text-xs px-2 py-1 bg-secondary/50 rounded-full text-text-muted"> ${getCuisineName(recipe.cuisine)} </span> <span class="text-xs px-2 py-1 bg-accent/20 rounded-full text-accent"> ${getTimeLabel(recipe.time)} </span> </div> <h3 class="font-bold text-lg text-text mb-1">${recipe.title}</h3> <div class="flex items-center justify-between text-sm text-text-muted"> <span class="flex items-center gap-1"> <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path> </svg>
$${recipe.budget} </span> <span class="flex items-center gap-1"> <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path> </svg> ${recipe.serves} 人
</span> <span class="flex items-center gap-1"> <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path> </svg> ${recipe.time} min
</span> </div> ${recipe.dietary.length > 0 && renderTemplate`<div class="flex flex-wrap gap-1 mt-2"> ${recipe.dietary.map((d) => renderTemplate`<span class="text-xs px-2 py-0.5 bg-accent/10 rounded-full text-accent"> ${d} </span>`)} </div>`} </div> </a> </article>`)} </div> <!-- No Results --> <div id="noResults" class="hidden text-center py-12"> <div class="text-6xl mb-4">🍽️</div> <h3 class="text-xl font-semibold text-text mb-2">No recipes found</h3> <p class="text-text-muted">Try adjusting your filters</p> </div> </div> </section>  <section id="about" class="bg-secondary/30 py-16"> <div class="max-w-7xl mx-auto px-4"> <div class="max-w-2xl mx-auto text-center"> <h2 class="text-3xl font-bold text-text mb-4">關於呢個網站</h2> <p class="text-text-muted mb-6">
為香港繁忙既OL同打工仔提供Quick Recipes,
          搵啱Budget、啱Time既食譜。
          適合關注健康既朋友，包括糖尿病患者。
</p> <div class="flex flex-wrap justify-center gap-4 text-sm"> <span class="px-3 py-1 bg-card-bg rounded-full text-text-muted">
🌿 健康食材
</span> <span class="px-3 py-1 bg-card-bg rounded-full text-text-muted">
⏱️ 30分鐘內
</span> <span class="px-3 py-1 bg-card-bg rounded-full text-text-muted">
💰 Budget friendly
</span> <span class="px-3 py-1 bg-card-bg rounded-full text-text-muted">
🏥 糖尿病友好
</span> </div> </div> </div> </section>  <button id="scrollTopBtn" class="fixed bottom-6 right-4 sm:bottom-8 sm:right-8 w-11 h-11 bg-primary text-white rounded-full shadow-lg flex items-center justify-center opacity-0 pointer-events-none transition-all duration-300 hover:bg-primary/90 z-40" aria-label="Scroll to top"> <span class="text-lg">⬆</span> </button>  <footer class="bg-text text-background py-8"> <div class="max-w-7xl mx-auto px-4 text-center"> <p>今晚煮咩好？© 2026Dinner Recipe HK</p> </div> </footer> ` }), defineScriptVars({ cuisines, dietaryOptions, timeOptions, servingOptions }));
}, "/Users/kate/Desktop/Kate/opencode/dinner-recipe/src/pages/index.astro", void 0);

const $$file = "/Users/kate/Desktop/Kate/opencode/dinner-recipe/src/pages/index.astro";
const $$url = "/-IThund-dinner-recipe.html";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Index,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
